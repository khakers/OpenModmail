services:
  bot:
    image: ghcr.io/khakers/openmodmail:develop
    build:
        context: .
        dockerfile: Dockerfile
    # env_file:
    #   - .env
    environment:
      - CONNECTION_URI=mongodb://mongo
    depends_on:
      - mongo

volumes:
  mongodb:
